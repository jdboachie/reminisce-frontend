/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "picsum.photos",
      "source.unsplash.com"
    ],
  },
}

module.exports = nextConfig 